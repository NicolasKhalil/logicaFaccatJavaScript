function formaTriangulo(a, b, c) {
  if (a < b + c && b < a + c && c < a + b) {
    return "Forma um triângulo";
  } else {
    return "Não forma um triângulo";
  }
}

// Exemplo:
console.log(formaTriangulo(3, 4, 5)); // Saída: Forma um triângulo
