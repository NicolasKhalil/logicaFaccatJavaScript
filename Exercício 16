function custoMacas(quantidade) {
  const preco = quantidade < 12 ? 1.30 : 1.00;
  return quantidade * preco;
}

// Exemplo:
console.log(custoMacas(10)); // Saída: 13
console.log(custoMacas(12)); // Saída: 12
